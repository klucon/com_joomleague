# Czech stage terminology correction

## Scope

- Replaced the incorrect machine translation `Typ jeviste` with `Typ faze`.
- Standardized related stage strings on the Czech term `faze` instead of `etapa` or `jeviste`.
- Corrected directly related machine translations for round labels and validation messages.

## Verification

- The Czech language file no longer contains `jeviste` terminology.
- Architecture verification passes.
- The deployed stage edit form displays `Typ faze`.
